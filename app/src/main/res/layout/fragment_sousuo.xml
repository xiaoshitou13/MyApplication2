<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="Fragments.SousuoFragment">

     <include android:id="@+id/includ" layout="@layout/includes" ></include>


     <ListView
         android:layout_width="100dp"
         android:layout_height="match_parent"
         android:id="@+id/lv"
         android:layout_below="@+id/includ"></ListView>

     <FrameLayout
         android:layout_width="match_parent"
         android:layout_height="match_parent"
         android:id="@+id/fragment"
         android:layout_below="@+id/includ"
         android:layout_toRightOf="@+id/lv"></FrameLayout>
</RelativeLayout>